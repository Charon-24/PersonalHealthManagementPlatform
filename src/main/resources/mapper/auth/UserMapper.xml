<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.peace.personalhealthmanagementplatform.module.auth.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.peace.personalhealthmanagementplatform.module.auth.entity.UserPO">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="real_name" property="realName"/>
        <result column="age" property="age"/>
        <result column="sex" property="sex"/>
        <result column="avatar" property="avatar"/>
        <result column="status" property="status"/>
        <result column="deleted" property="deleted"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, username, password, phone, email, real_name, age, sex, avatar, status, deleted, create_time, update_time
    </sql>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM `user`
        WHERE username = #{username} AND deleted = 0
    </select>

    <select id="selectByPhone" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM `user`
        WHERE phone = #{phone} AND deleted = 0
    </select>

    <select id="selectByEmail" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM `user`
        WHERE email = #{email} AND deleted = 0
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM `user`
        WHERE id = #{id} AND deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (username, password, phone, email, status, deleted, create_time)
        VALUES (#{username}, #{password}, #{phone}, #{email}, 1, 0, NOW(6))
    </insert>

    <update id="updatePassword">
        UPDATE `user`
        SET password = #{password}, update_time = NOW(6)
        WHERE id = #{id} AND deleted = 0
    </update>

</mapper>
